<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contactos</title>
    <style>
        .title{
            text-align: center;
        }
        .list{
            width: 1200px;
            margin: auto;
            display: flex;
            flex-direction: column;
            list-style: none;
        }
        .row{
            display: flex;
            flex-direction: row;
            align-items: center;
            width: 400px;
            min-height: 60px;
            border: 1px solid rgb(190, 190, 190);
            border-radius: 5px;
            margin-bottom: 12px;
            margin-left: auto;
            margin-right: auto;
            background-color: rgb(40, 151, 20);
            padding: 20px;
        }
        .row > strong {
            margin-right: 12px;
        }

        .empty{
            background-color: rgb(190, 190, 190) !important;
            justify-content: center;
        }
    </style>
</head>
<body>
    <h1 class="title">CONTACTOS</h1>
    <ul class="list">
        {{#tieneContactos}}
        {{#contactos}}
                <li class="row"><strong>{{nombre}} :</strong><span>{{telefono}}</span></li>
        {{/contactos}}
        {{/tieneContactos}}
        {{^tieneContactos}}
            <li class="row empty">No hay contactos</li>
        {{/tieneContactos}}
    </ul>
</body>
</html>